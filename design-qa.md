# TraceHalo Design QA

final result: passed

## 本轮补充：设置选择列表的真实可操作性

- 用户问题参考图：`/var/folders/0w/cnq2c8gs1n7dwvq33lvpd2l00000gn/T/codex-clipboard-bb15652c-7f8a-4439-833f-cdd343d0ed8a.png`，1900 × 496 px；状态为深色外观、英文、设置页底部折叠行。
- 最终全页快照：`.build/ui-qa/10-settings.png`，3116 × 2020 px，对应 1558 × 1010 pt 的 2× 离屏画布；状态为深色外观、简体中文、有电池、全部选择器折叠。
- 最终交互截图：`.build/settings-list-qa/03-final-language-list.png`，1163 × 768 px；状态为实际 App 窗口、简体中文、“应用语言”列表已展开。
- 持久化截图：`.build/settings-list-qa/04-five-seconds-after-relaunch.png`；英文说明页截图：`.build/settings-list-qa/05-final-english-diagnostics.png`。
- 参考图、全页快照和展开列表截图已在同一视觉 QA 输入中对比；折叠态继续沿用原来的图标、标题、当前值、分隔线和 chevron 层级，没有引入新的卡片样式或布局跳动。

### 交互与可访问性结论

- “应用语言”“外观”“温度单位”“打开时进入”“历史数据保留”均改为可见的实体 `Button`；点击后由 `popover` 展示纵向选项列表，选中项显示 checkmark，不再使用覆盖整行的透明 `Menu`。
- 实机 Accessibility Tree 中五行均暴露为“按钮”，语言弹窗中的“跟随系统 / English / 简体中文”也分别暴露为按钮；鼠标点击和辅助功能点击使用同一交互路径。
- 实机选择 English 后，主设置页和“数据状态与故障排查”说明页同步切换为英文；选择 5 秒刷新并结束进程后重启，页面仍显示 `Updates every 5 seconds`，不会再被无电池设备的预设模式覆盖。
- 重复启动测试只保留一个进程；第二个实例立即退出并激活已有窗口。最终 P0/P1/P2 为 0。

### 本轮 QA 历史

1. 复现并确认透明 `Menu` 触发层在实际 App 窗口中会吞掉或错配点击，虽然离屏调试可以展示菜单，但不能代表用户鼠标路径可用。
2. 将选择行替换为可见按钮和列表 popover，逐项验证语言、外观、温度单位、打开页面和历史保留入口。
3. 补齐弹窗的语言环境注入，修复切换语言后说明页仍使用旧语言的问题。
4. 移除无电池设备对高级自定义 5 秒值的自动归一化；完成进程级重启验证。
5. 完成最终 1558 × 1010 pt 快照、实际窗口展开态检查和参考图同输入对比，结果通过。

## 本轮：设置页面、设备能力与固定窗口

- 参考图：`/var/folders/0w/cnq2c8gs1n7dwvq33lvpd2l00000gn/T/codex-clipboard-02b3877e-b5a4-48ac-bee3-043aacebd843.png`，1559 × 1009 px。
- 实现快照：`.build/ui-qa/10-settings.png`，3116 × 2020 px（1558 × 1010 pt，2×）；状态为简体中文、深色外观、有电池、均衡模式。
- 无电池快照：`.build/ui-qa/10-settings-no-battery.png`；浅色英文快照：`.build/ui-qa/10-settings-light-en.png`。
- 最终对比图：`.build/design-qa/settings/settings-comparison-final.png`。对比时裁去参考图 37 px 原生标题栏，并将两侧统一为 972 px 内容高度。

### 最终对比结论

- 标题、重置入口、使用方式卡片、当前设置说明、左右两列面板、分隔线和底部边界与参考图采用同一层级、顺序和比例。
- 选择行已从原生 `Menu` 的压缩标签中拆出独立视觉层；标题、当前值和 chevron 均完整可见，整行仍可点击并保留无障碍名称和值。
- 启动与菜单栏、隐私开关、语言、外观、温度单位、打开页面、诊断、高级设置、关于和恢复默认均连接真实状态或操作。
- “更多”新增“历史数据保留”全行选择器，3/7/14/30 天选项与当前值、chevron 均完整可见，默认显示 3 天。
- 无电池设备只保留“均衡”和“更实时”两张等宽卡片，并移除全部电池说明；英文与浅色状态没有截断、重叠或错误本地化 key。
- 参考图的天平图标在当前系统 SF Symbols 中没有同形符号，实现使用语义最接近的 `scalemass`；离屏快照中的原生开关使用非活动窗口灰色，实际活动窗口仍由系统 accent tint 显示。这两项为 P3 平台差异，不影响功能或布局。

### QA 历史

1. 首轮发现 borderless `Menu` 把自定义行压缩并统一染蓝，导致当前值和 chevron 消失。
2. 曾改为透明全行菜单触发层以恢复主文字、灰色值和 chevron，但后续实机鼠标验证发现该方案会导致用户无法展开选择项，已在本轮替换为可见按钮与列表 popover。
3. 按同尺寸对比收敛标题水平位置、使用方式卡片高度、信息条和下方紧凑行高，使两列底部重新对齐。
4. 修复 snapshot 资源包指向旧 App 的问题后，重新验证简体中文、无电池和英文浅色三种状态；最终 P0/P1/P2 均为 0。

## 本轮：固定窗口、系统工具与存储页面

- 参考图：`/var/folders/0w/cnq2c8gs1n7dwvq33lvpd2l00000gn/T/codex-clipboard-7d8ce63b-df11-4c9c-afc8-2b10da5eecb1.png`
- 参考图尺寸：1559 × 1009 px，深色外观
- 同尺寸实现快照：`.build/ui-qa/05-storage.png`，1558 × 1010 px
- 已安装版本实机截图：`.build/design-qa/storage/installed-storage-final.jpeg`，1163 × 768 px
- 最终实机对比图：`.build/design-qa/storage/storage-comparison-installed-final.png`
- 同尺寸全画布对比图：`.build/design-qa/storage/storage-comparison-final.png`

### 聚焦对比

- 页面头部、卷选择器、容量环、可用空间、三段容量图例和健康区与参考图保持同一信息层级。
- I/O 区按参考图保留读取、写入、IOPS、时间范围和双轴实时折线；曲线使用真实采样，不生成示例数据。
- 右侧“卷与设备”按参考图排列；未知值显示“系统未提供”，不会把缺失信息伪装为外置、未加密或固定存储。
- 底部“累计 I/O 与设备标识”可点击打开详情，保持参考图的单行入口结构。
- SMART 语义采用否定优先分类；故障或警告不会因数据可读取而显示绿色正常。

### 全画布与响应式对比

- 正常屏幕使用固定 1320 × 840 内容画布；小屏使用固定 1180 × 750 紧凑画布。页面切换不会改变窗口尺寸。
- 实机在紧凑画布下从概览、存储、启动优化到应用管理保持相同窗口边界，无横向裁剪。
- 侧栏调整为 184 pt，未选文字使用高对比主色，子项减小缩进并完整显示；这是针对用户反馈的可读性修正。
- 参考图与实机图放入同一对比画布后复核：主要分区、卡片比例、对齐、边框、圆角和信息顺序一致；紧凑画布下字体与间距按比例收敛。

### 加载与动态状态

- 启动优化和应用管理使用后台预载；首次进入显示固定几何骨架，缓存刷新保留旧数据并显示轻量状态，不再让界面跳动。
- 存储 I/O 使用 60 秒、最多 64 点的内存历史；最新数据变化触发尾段动画，开启“减少动态效果”后直接更新。
- SMART/健康信息独立后台低频刷新，默认 5 分钟，不进入 2 秒遥测链。

### QA 历史

1. 初版同尺寸快照暴露了截图缩放口径与实机窗口密度不同。
2. 增加已安装版本实机截图，并将参考图适配到同一实机窗口尺寸后重新并排检查。
3. 修复健康语义不一致、稳定点数时折线不再动画、未知卷属性误判、长卷名宽度、跨屏固定画布和 Reduce Motion。
4. 最终实机检查通过：存储、启动优化、应用管理和侧栏均可正常操作，窗口尺寸稳定。

## 既有：实时监控页面

- 参考图：`codex-clipboard-2bcc3f94-bd0a-4c26-a375-6be1702509dd.png`
- 实现快照：`.build/ui-qa/02-monitor.png`
- 对比图：`.build/ui-qa/monitor-target-comparison.png`
- 对比视口：1558 × 1010
- 配置区、实时预览和详情列已实现；模块点击切换详情，重复点击不会收起。
- 共享 2 秒采样驱动数字、进度条和历史图，没有新增高频计时器。
- 无可靠整机输入功率且设备没有电池时，Power 模块按设备能力隐藏。
